// you can write js here
console.log('exo-3');

var playerInput = prompt("Votre choix ( pierre , feuille, ciseau) ?")
var error = "Erreur"

getPlayerChoice(playerInput)
    if (playerInput){
    console.log(playerInput)
}else{
    console.log(error)
}


getComputerChoice(){
    function Math.random(2)
    return playerInput()
}

function findWinner(getPlayerChoice , getComputerChoice){
    if (joueur===robot) {
        resultat = "Egalité";
      }
      else if ((joueur === "Pierre" && robot === "Ciseaux") || (joueur === "Ciseaux" && robot === "Feuilles") || (joueur === "Feuilles") && (robot === "Pierre")) {
        resultat = "Gagné";
      }
      else {
        resultat = "Perdu";
      }
}

function playGame(){
    var uChoice = getPlayerChoice('pierre')
    var computerChoice = getComputerChoice()
}

console.log(playerInput.toLowerCase())